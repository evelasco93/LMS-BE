{
  "name": "lms-mvp-ev",
  "version": "0.0.1",
  "description": "Lead Management System MVP",
  "scripts": {
    "build": "tsc && npm run build:handlers",
    "build:clients": "cd handlers/clients && tsc",
    "build:affiliates": "cd handlers/affiliates && tsc",
    "build:handlers": "npm run build:clients && npm run build:affiliates",
    "test": "./scripts/run-handler-tests.sh"
  },
  "author": "Edgar Velasco",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-lambda": "^3.994.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/util-dynamodb": "^3.600.0",
    "aws-cdk-lib": "^2.160.0",
    "constructs": "^10.3.0",
    "inversify": "^6.0.2",
    "reflect-metadata": "^0.2.2",
    "ts-lambda-api": "^2.5.3",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.19.13",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^2.1.0",
    "aws-cdk": "^2.160.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
